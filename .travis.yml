language: node_js
node_js:
    - 4
    - 6
    - 7
    - 8
cache:
  directories:
    - node_modules
script:
    - yarn lint
    - yarn test
    - yarn coverage
after_script: yarn add coveralls && yarn coverage:report | coveralls
